/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { Security } from "../models/security.js";
import { ClosedEnum } from "../types/enums.js";
import { HTTPClient } from "./http.js";
import { Logger } from "./logger.js";
import { RetryConfig } from "./retries.js";
import { Params, pathToFunc } from "./url.js";

/**
 * Contains the list of servers available to the SDK
 */
export const ServerList = [
  /**
   * APIM Management API v2 - Default base URL
   */
  "{protocol}://{managementAPIHost}/management/v2",
  /**
   * APIM Management API v2 - Base URL to target specific organizations
   */
  "{protocol}://{managementAPIHost}/management/v2/organizations/{orgId}",
] as const;

/**
 * The protocol you want to use to communicate with the mAPI
 */
export const ServerProtocol = {
  Https: "https",
  Http: "http",
} as const;
/**
 * The protocol you want to use to communicate with the mAPI
 */
export type ServerProtocol = ClosedEnum<typeof ServerProtocol>;

export type SDKOptions = {
  /**
   * The security details required to authenticate the SDK
   */
  security?: Security | (() => Promise<Security>) | undefined;

  httpClient?: HTTPClient;
  /**
   * Allows overriding the default server used by the SDK
   */
  serverIdx?: number | undefined;
  /**
   * Sets the protocol variable for url substitution
   */
  protocol?: ServerProtocol | undefined;
  /**
   * Sets the managementAPIHost variable for url substitution
   */
  managementAPIHost?: string | undefined;
  /**
   * Sets the orgId variable for url substitution
   */
  orgId?: string | undefined;
  /**
   * Allows overriding the default server URL used by the SDK
   */
  serverURL?: string | undefined;
  /**
   * Allows overriding the default user agent used by the SDK
   */
  userAgent?: string | undefined;
  /**
   * Allows overriding the default retry config used by the SDK
   */
  retryConfig?: RetryConfig;
  timeoutMs?: number;
  debugLogger?: Logger | undefined;
};

export function serverURLFromOptions(options: SDKOptions): URL | null {
  let serverURL = options.serverURL;

  const serverParams: Params[] = [
    {
      "protocol": options.protocol ?? "https",
      "managementAPIHost": options.managementAPIHost ?? "localhost:8083",
    },
    {
      "protocol": options.protocol ?? "https",
      "managementAPIHost": options.managementAPIHost ?? "localhost:8083",
      "orgId": options.orgId ?? "DEFAULT",
    },
  ];
  let params: Params = {};

  if (!serverURL) {
    const serverIdx = options.serverIdx ?? 0;
    if (serverIdx < 0 || serverIdx >= ServerList.length) {
      throw new Error(`Invalid server index ${serverIdx}`);
    }
    serverURL = ServerList[serverIdx] || "";
    params = serverParams[serverIdx] || {};
  }

  const u = pathToFunc(serverURL)(params);
  return new URL(u);
}

export const SDK_METADATA = {
  language: "typescript",
  openapiDocVersion: "2.0.0",
  sdkVersion: "0.0.1",
  genVersion: "2.698.4",
  userAgent: "speakeasy-sdk/mcp-typescript 0.0.1 2.698.4 2.0.0 gravitee-apim",
} as const;
