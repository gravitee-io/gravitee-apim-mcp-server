/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiDocumentationGetAPIPages } from "../../funcs/apiDocumentationGetAPIPages.js";
import { GetApiPagesRequest$zodSchema } from "../../models/getapipagesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiPagesRequest$zodSchema,
};

export const tool$apiDocumentationGetAPIPages: ToolDefinition<typeof args> = {
  name: "API-documentation-get-api-pages",
  description: `Get API documentation pages

Get API documentation pages.
When no parentId is specified, returns all the pages of the API.

When parentId is specified (either ID of a folder, or 'ROOT'), only the pages with the given parent are returned, 
and the breadcrumb of the specified folder is added to the response. 

User must have the API_DOCUMENTATION[READ] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiDocumentationGetAPIPages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
