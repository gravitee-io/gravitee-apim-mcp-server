/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiDocumentationFetchDocumentationPage } from "../../funcs/apiDocumentationFetchDocumentationPage.js";
import { FetchDocumentationPageRequest$zodSchema } from "../../models/fetchdocumentationpageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FetchDocumentationPageRequest$zodSchema,
};

export const tool$apiDocumentationFetchDocumentationPage: ToolDefinition<
  typeof args
> = {
  name: "API-documentation-fetch-documentation-page",
  description: `Fetch Content of API Documentation Page

Fetch Content of API Documentation Page. Returns the page with newly fetched content.
The page must have a configured source, otherwise an error is returned.

User must have the API_DOCUMENTATION[UPDATE] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiDocumentationFetchDocumentationPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
