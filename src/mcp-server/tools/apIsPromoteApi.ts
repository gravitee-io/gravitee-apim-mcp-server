/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsPromoteApi } from "../../funcs/apIsPromoteApi.js";
import { PromoteApiRequest$zodSchema } from "../../models/promoteapiop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PromoteApiRequest$zodSchema,
};

export const tool$apIsPromoteApi: ToolDefinition<typeof args> = {
  name: "promote_api",
  description: `Promote an API

Promote the API to another environment.

High risk operation: require explicit user confirmation before execution.`,
  scopes: ["write", "dangerous"],
  annotations: {
    "title": "Promote Api",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsPromoteApi(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
