/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiAnalyticsGetNativeAPILogsSummary } from "../../funcs/apiAnalyticsGetNativeAPILogsSummary.js";
import { GetNativeApiLogsSummaryRequest$zodSchema } from "../../models/getnativeapilogssummaryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetNativeApiLogsSummaryRequest$zodSchema,
};

export const tool$apiAnalyticsGetNativeAPILogsSummary: ToolDefinition<
  typeof args
> = {
  name: "get_native_api_logs_summary",
  description: `Get analytics for native API connection logs

Returns the count of native API connection lifecycle events grouped by connection status.`,
  scopes: ["read"],
  annotations: {
    "title": "Get Native Api Logs Summary",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiAnalyticsGetNativeAPILogsSummary(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
