/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiAnalyticsGetNativeAPILogs } from "../../funcs/apiAnalyticsGetNativeAPILogs.js";
import { GetNativeApiLogsRequest$zodSchema } from "../../models/getnativeapilogsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetNativeApiLogsRequest$zodSchema,
};

export const tool$apiAnalyticsGetNativeAPILogs: ToolDefinition<typeof args> = {
  name: "get_native_api_logs",
  description: `Get native API connection logs

Returns the paginated list of native API connection log entries for the API.`,
  scopes: ["read"],
  annotations: {
    "title": "Get Native Api Logs",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiAnalyticsGetNativeAPILogs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
