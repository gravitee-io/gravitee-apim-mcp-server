/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsCreateApiWithImportDefinitionFromUrl } from "../../funcs/apIsCreateApiWithImportDefinitionFromUrl.js";
import { CreateApiWithImportDefinitionFromUrlRequest$zodSchema } from "../../models/createapiwithimportdefinitionfromurlop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateApiWithImportDefinitionFromUrlRequest$zodSchema,
};

export const tool$apIsCreateApiWithImportDefinitionFromUrl: ToolDefinition<
  typeof args
> = {
  name: "create_api_with_import_definition_from_url",
  description: `Import API definition from a remote URL

⚠️ Support only v4 API for the moment. ⚠️<br> Create an API by fetching an API definition from a remote URL.<br> The URL must be permitted by the configured import whitelist.`,
  scopes: ["write"],
  annotations: {
    "title": "Create Api With Import Definition From Url",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsCreateApiWithImportDefinitionFromUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
