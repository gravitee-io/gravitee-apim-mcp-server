/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsSearchGroups } from "../../funcs/groupsSearchGroups.js";
import { SearchGroupsRequest$zodSchema } from "../../models/searchgroupsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SearchGroupsRequest$zodSchema,
};

export const tool$groupsSearchGroups: ToolDefinition<typeof args> = {
  name: "search_groups",
  description: `Search for groups using filters

Search for groups using filters.`,
  scopes: ["read"],
  annotations: {
    "title": "Search Groups",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupsSearchGroups(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
