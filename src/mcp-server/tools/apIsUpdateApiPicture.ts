/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsUpdateApiPicture } from "../../funcs/apIsUpdateApiPicture.js";
import { UpdateApiPictureRequest$zodSchema } from "../../models/updateapipictureop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateApiPictureRequest$zodSchema,
};

export const tool$apIsUpdateApiPicture: ToolDefinition<typeof args> = {
  name: "AP-is-update-api-picture",
  description: `Update an API's picture

Update the API's picture.

User must have the API_DEFINITION[UPDATE] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsUpdateApiPicture(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
