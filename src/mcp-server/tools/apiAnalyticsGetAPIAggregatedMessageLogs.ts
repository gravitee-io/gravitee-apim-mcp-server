/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiAnalyticsGetAPIAggregatedMessageLogs } from "../../funcs/apiAnalyticsGetAPIAggregatedMessageLogs.js";
import { GetApiAggregatedMessageLogsRequest$zodSchema } from "../../models/getapiaggregatedmessagelogsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiAggregatedMessageLogsRequest$zodSchema,
};

export const tool$apiAnalyticsGetAPIAggregatedMessageLogs: ToolDefinition<
  typeof args
> = {
  name: "get_api_aggregated_message_logs",
  description:
    `Get API Aggregated Messages logs (with endpoint and entrypoint messages)

Get API Aggregated Messages logs (with endpoint and entrypoint messages).`,
  scopes: ["read"],
  annotations: {
    "title": "Get Api Aggregated Message Logs",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiAnalyticsGetAPIAggregatedMessageLogs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
