/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsGetApi } from "../../funcs/apIsGetApi.js";
import { GetApiRequest$zodSchema } from "../../models/getapiop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiRequest$zodSchema,
};

export const tool$apIsGetApi: ToolDefinition<typeof args> = {
  name: "get_api",
  description: `Get a specific API

Get a specific API.`,
  scopes: ["read"],
  annotations: {
    "title": "Get Api",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsGetApi(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
