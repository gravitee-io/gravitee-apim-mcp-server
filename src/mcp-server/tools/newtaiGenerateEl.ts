/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { newtaiGenerateEl } from "../../funcs/newtaiGenerateEl.js";
import { GenerateElRequest$zodSchema } from "../../models/generateelop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GenerateElRequest$zodSchema,
};

export const tool$newtaiGenerateEl: ToolDefinition<typeof args> = {
  name: "generate_el",
  description: `Generate Expression Language

Generate Expression Language.`,
  scopes: ["write"],
  annotations: {
    "title": "Generate El",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await newtaiGenerateEl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
