/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiSpecGenPost } from "../../funcs/apiSpecGenPost.js";
import { PostRequest$zodSchema } from "../../models/postop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostRequest$zodSchema,
};

export const tool$apiSpecGenPost: ToolDefinition<typeof args> = {
  name: "post",
  description: `Starts the API SpecGen`,
  scopes: ["read"],
  annotations: {
    "title": "Post",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiSpecGenPost(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
