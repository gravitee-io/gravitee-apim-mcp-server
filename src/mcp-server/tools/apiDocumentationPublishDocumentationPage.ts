/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiDocumentationPublishDocumentationPage } from "../../funcs/apiDocumentationPublishDocumentationPage.js";
import { PublishDocumentationPageRequest$zodSchema } from "../../models/publishdocumentationpageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PublishDocumentationPageRequest$zodSchema,
};

export const tool$apiDocumentationPublishDocumentationPage: ToolDefinition<
  typeof args
> = {
  name: "API-documentation-publish-documentation-page",
  description: `Publish API Documentation Page

Publish API Documentation Page. Returns the published page.

User must have the API_DOCUMENTATION[UPDATE] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiDocumentationPublishDocumentationPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
