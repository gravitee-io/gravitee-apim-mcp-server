/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiAnalyticsGetAPILog } from "../../funcs/apiAnalyticsGetAPILog.js";
import { GetApiLogRequest$zodSchema } from "../../models/getapilogop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiLogRequest$zodSchema,
};

export const tool$apiAnalyticsGetAPILog: ToolDefinition<typeof args> = {
  name: "get_api_log",
  description: `Get API log for a request

Get API log for a request.`,
  scopes: ["read"],
  annotations: {
    "title": "Get Api Log",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiAnalyticsGetAPILog(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
