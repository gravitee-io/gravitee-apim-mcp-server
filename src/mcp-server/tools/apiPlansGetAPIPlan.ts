/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiPlansGetAPIPlan } from "../../funcs/apiPlansGetAPIPlan.js";
import { GetApiPlanRequest$zodSchema } from "../../models/getapiplanop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiPlanRequest$zodSchema,
};

export const tool$apiPlansGetAPIPlan: ToolDefinition<typeof args> = {
  name: "get_api_plan",
  description: `Get one API's plan

Get the API's plan by its identifier.`,
  scopes: ["read"],
  annotations: {
    "title": "Get Api Plan",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiPlansGetAPIPlan(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
