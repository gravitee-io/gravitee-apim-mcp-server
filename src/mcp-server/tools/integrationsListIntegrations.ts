/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListIntegrations } from "../../funcs/integrationsListIntegrations.js";
import { ListIntegrationsRequest$zodSchema } from "../../models/listintegrationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListIntegrationsRequest$zodSchema,
};

export const tool$integrationsListIntegrations: ToolDefinition<typeof args> = {
  name: "list_integrations",
  description: `List Integrations

Get the list of Integration for a specific environment.<br> The results are paginated.`,
  scopes: ["read"],
  annotations: {
    "title": "List Integrations",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsListIntegrations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
