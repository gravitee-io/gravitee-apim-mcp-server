/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiDocumentationUpdateDocumentationPage } from "../../funcs/apiDocumentationUpdateDocumentationPage.js";
import { UpdateDocumentationPageRequest$zodSchema } from "../../models/updatedocumentationpageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateDocumentationPageRequest$zodSchema,
};

export const tool$apiDocumentationUpdateDocumentationPage: ToolDefinition<
  typeof args
> = {
  name: "API-documentation-update-documentation-page",
  description: `Update API documentation page

Update API documentation. The result will be the updated documentation.
Only MARKDOWN and FOLDER types are supported.

User must have the API_DOCUMENTATION[CREATE] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiDocumentationUpdateDocumentationPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
