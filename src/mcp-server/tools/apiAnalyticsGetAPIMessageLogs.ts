/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiAnalyticsGetAPIMessageLogs } from "../../funcs/apiAnalyticsGetAPIMessageLogs.js";
import { GetApiMessageLogsRequest$zodSchema } from "../../models/getapimessagelogsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiMessageLogsRequest$zodSchema,
};

export const tool$apiAnalyticsGetAPIMessageLogs: ToolDefinition<typeof args> = {
  name: "API-analytics-get-api-message-logs",
  description: `Get API Messages logs

Get API Messages logs.

User must have the API_LOG[READ] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiAnalyticsGetAPIMessageLogs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
