/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiSubscriptionsCloseAPISubscription } from "../../funcs/apiSubscriptionsCloseAPISubscription.js";
import { CloseApiSubscriptionRequest$zodSchema } from "../../models/closeapisubscriptionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CloseApiSubscriptionRequest$zodSchema,
};

export const tool$apiSubscriptionsCloseAPISubscription: ToolDefinition<
  typeof args
> = {
  name: "close_api_subscription",
  description: `Close one API's subscription

Close the API's subscription.

High risk operation: require explicit user confirmation before execution.`,
  scopes: ["write", "dangerous"],
  annotations: {
    "title": "Close Api Subscription",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiSubscriptionsCloseAPISubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
