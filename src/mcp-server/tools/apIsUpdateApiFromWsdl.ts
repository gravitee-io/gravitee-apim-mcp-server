/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsUpdateApiFromWsdl } from "../../funcs/apIsUpdateApiFromWsdl.js";
import { UpdateApiFromWsdlRequest$zodSchema } from "../../models/updateapifromwsdlop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateApiFromWsdlRequest$zodSchema,
};

export const tool$apIsUpdateApiFromWsdl: ToolDefinition<typeof args> = {
  name: "update_api_from_wsdl",
  description: `Update API from WSDL descriptor

Update an existing API by importing a WSDL descriptor.<br> The descriptor payload must be a valid WSDL 1.1 document, either as inline content or a remote URL.`,
  scopes: ["write"],
  annotations: {
    "title": "Update Api From Wsdl",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsUpdateApiFromWsdl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
