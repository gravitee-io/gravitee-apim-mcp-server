/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsUpdateApiFromSwagger } from "../../funcs/apIsUpdateApiFromSwagger.js";
import { UpdateApiFromSwaggerRequest$zodSchema } from "../../models/updateapifromswaggerop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateApiFromSwaggerRequest$zodSchema,
};

export const tool$apIsUpdateApiFromSwagger: ToolDefinition<typeof args> = {
  name: "update_api_from_swagger",
  description: `Update API from OpenAPI descriptor

Update an existing API by importing an OpenAPI (Swagger) descriptor.<br> The descriptor payload must be a valid OpenAPI 2.x or 3.x document in JSON or YAML format.`,
  scopes: ["write"],
  annotations: {
    "title": "Update Api From Swagger",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsUpdateApiFromSwagger(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
