/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsGetApiPicture } from "../../funcs/apIsGetApiPicture.js";
import { GetApiPictureRequest$zodSchema } from "../../models/getapipictureop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiPictureRequest$zodSchema,
};

export const tool$apIsGetApiPicture: ToolDefinition<typeof args> = {
  name: "AP-is-get-api-picture",
  description: `Get an API's picture

Get the API's picture.

User must have the API_DEFINITION[READ] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsGetApiPicture(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
