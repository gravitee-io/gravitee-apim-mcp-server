/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiEventsGetAPIEvents } from "../../funcs/apiEventsGetAPIEvents.js";
import { GetApiEventsRequest$zodSchema } from "../../models/getapieventsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiEventsRequest$zodSchema,
};

export const tool$apiEventsGetAPIEvents: ToolDefinition<typeof args> = {
  name: "API-events-get-api-events",
  description: `Get API Events

Get API Events.

User must have the API_EVENT[READ] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiEventsGetAPIEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
