/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsGetApiBackground } from "../../funcs/apIsGetApiBackground.js";
import { GetApiBackgroundRequest$zodSchema } from "../../models/getapibackgroundop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiBackgroundRequest$zodSchema,
};

export const tool$apIsGetApiBackground: ToolDefinition<typeof args> = {
  name: "AP-is-get-api-background",
  description: `Get an API's background picture

Get the API's background picture.

User must have the API_DEFINITION[READ] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsGetApiBackground(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
