/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiPlansPatchAPIPlan } from "../../funcs/apiPlansPatchAPIPlan.js";
import { PatchApiPlanRequest$zodSchema } from "../../models/patchapiplanop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PatchApiPlanRequest$zodSchema,
};

export const tool$apiPlansPatchAPIPlan: ToolDefinition<typeof args> = {
  name: "patch_api_plan",
  description: `Partially update one API's plan

Apply a partial update to an API's plan using JSON Merge Patch (RFC 7396) or JSON Patch (RFC 6902). Only v4 HTTP Proxy API plans support this operation. Only the following fields are patchable: name, description, security (configuration only),`,
  scopes: ["write"],
  annotations: {
    "title": "Patch Api Plan",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiPlansPatchAPIPlan(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
