/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiAnalyticsGetResponseStatusOvertime } from "../../funcs/apiAnalyticsGetResponseStatusOvertime.js";
import { GetResponseStatusOvertimeRequest$zodSchema } from "../../models/getresponsestatusovertimeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetResponseStatusOvertimeRequest$zodSchema,
};

export const tool$apiAnalyticsGetResponseStatusOvertime: ToolDefinition<
  typeof args
> = {
  name: "get_response_status_overtime",
  description: `Get API Analytics response status counts over time

Get API analytics response status counts over time.`,
  scopes: ["read"],
  annotations: {
    "title": "Get Response Status Overtime",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiAnalyticsGetResponseStatusOvertime(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
