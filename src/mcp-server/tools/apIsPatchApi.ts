/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsPatchApi } from "../../funcs/apIsPatchApi.js";
import { PatchApiRequest$zodSchema } from "../../models/patchapiop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PatchApiRequest$zodSchema,
};

export const tool$apIsPatchApi: ToolDefinition<typeof args> = {
  name: "patch_api",
  description: `Patch a V4 HTTP Proxy API

Partially update a V4 HTTP Proxy API using JSON Patch (RFC 6902) or JSON Merge Patch (RFC 7396). Only the following fields are patchable: name, description, apiVersion, visibility, labels, tags, lifecycleState, categories, groups, analytics, failover, flowExecution, flows, services, resources, endpointGroups,`,
  scopes: ["write"],
  annotations: {
    "title": "Patch Api",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsPatchApi(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
