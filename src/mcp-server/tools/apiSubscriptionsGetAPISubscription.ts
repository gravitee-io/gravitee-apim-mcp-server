/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiSubscriptionsGetAPISubscription } from "../../funcs/apiSubscriptionsGetAPISubscription.js";
import { GetApiSubscriptionRequest$zodSchema } from "../../models/getapisubscriptionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiSubscriptionRequest$zodSchema,
};

export const tool$apiSubscriptionsGetAPISubscription: ToolDefinition<
  typeof args
> = {
  name: "get_api_subscription",
  description: `Get one API's subscription

Get the API's subscription by its identifier.`,
  scopes: ["read"],
  annotations: {
    "title": "Get Api Subscription",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiSubscriptionsGetAPISubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
