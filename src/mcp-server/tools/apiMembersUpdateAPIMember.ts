/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiMembersUpdateAPIMember } from "../../funcs/apiMembersUpdateAPIMember.js";
import { UpdateApiMemberRequest$zodSchema } from "../../models/updateapimemberop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateApiMemberRequest$zodSchema,
};

export const tool$apiMembersUpdateAPIMember: ToolDefinition<typeof args> = {
  name: "update_api_member",
  description: `Updates a member for an API.

Edit a member for an API. Returns a 400 HTTP Error:`,
  scopes: ["write"],
  annotations: {
    "title": "Update Api Member",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiMembersUpdateAPIMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
