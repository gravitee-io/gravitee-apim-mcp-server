/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsIngestIntegration } from "../../funcs/integrationsIngestIntegration.js";
import { IngestIntegrationRequest$zodSchema } from "../../models/ingestintegrationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: IngestIntegrationRequest$zodSchema,
};

export const tool$integrationsIngestIntegration: ToolDefinition<typeof args> = {
  name: "integrations-ingest-integration",
  description: `Ingest APIs for a specific Integration

Ingest all APIs for a specific Integration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsIngestIntegration(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
