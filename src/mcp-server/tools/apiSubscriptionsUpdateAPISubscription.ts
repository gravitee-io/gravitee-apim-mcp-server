/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiSubscriptionsUpdateAPISubscription } from "../../funcs/apiSubscriptionsUpdateAPISubscription.js";
import { UpdateApiSubscriptionRequest$zodSchema } from "../../models/updateapisubscriptionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateApiSubscriptionRequest$zodSchema,
};

export const tool$apiSubscriptionsUpdateAPISubscription: ToolDefinition<
  typeof args
> = {
  name: "update_api_subscription",
  description: `Update one API's subscription

Update the API's subscription.`,
  scopes: ["write"],
  annotations: {
    "title": "Update Api Subscription",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiSubscriptionsUpdateAPISubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
