/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiAnalyticsGetAPILogErrorKeys } from "../../funcs/apiAnalyticsGetAPILogErrorKeys.js";
import { GetApiLogErrorKeysRequest$zodSchema } from "../../models/getapilogerrorkeysop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiLogErrorKeysRequest$zodSchema,
};

export const tool$apiAnalyticsGetAPILogErrorKeys: ToolDefinition<typeof args> =
  {
    name: "get_api_log_error_keys",
    description: `Get API log error keys

Returns a list of distinct error keys found in connection logs for the specified API.`,
    scopes: ["read"],
    annotations: {
      "title": "Get Api Log Error Keys",
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await apiAnalyticsGetAPILogErrorKeys(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
