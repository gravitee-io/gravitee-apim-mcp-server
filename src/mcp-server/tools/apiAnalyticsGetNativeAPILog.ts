/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiAnalyticsGetNativeAPILog } from "../../funcs/apiAnalyticsGetNativeAPILog.js";
import { GetNativeApiLogRequest$zodSchema } from "../../models/getnativeapilogop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetNativeApiLogRequest$zodSchema,
};

export const tool$apiAnalyticsGetNativeAPILog: ToolDefinition<typeof args> = {
  name: "get_native_api_log",
  description: `Get a native API connection log for a request

Returns the native API connection log entry for the given request.`,
  scopes: ["read"],
  annotations: {
    "title": "Get Native Api Log",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiAnalyticsGetNativeAPILog(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
