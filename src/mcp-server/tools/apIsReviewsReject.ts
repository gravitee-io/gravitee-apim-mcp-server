/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsReviewsReject } from "../../funcs/apIsReviewsReject.js";
import { ReviewsRejectRequest$zodSchema } from "../../models/reviewsrejectop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ReviewsRejectRequest$zodSchema,
};

export const tool$apIsReviewsReject: ToolDefinition<typeof args> = {
  name: "reviews_reject",
  description: `Reject a review

High risk operation: require explicit user confirmation before execution.`,
  scopes: ["write", "dangerous"],
  annotations: {
    "title": "Reviews Reject",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsReviewsReject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
