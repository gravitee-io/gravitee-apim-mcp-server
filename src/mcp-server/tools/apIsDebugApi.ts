/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apIsDebugApi } from "../../funcs/apIsDebugApi.js";
import { DebugApiRequest$zodSchema } from "../../models/debugapiop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DebugApiRequest$zodSchema,
};

export const tool$apIsDebugApi: ToolDefinition<typeof args> = {
  name: "debug_api",
  description: `Debug an API on gateway instances

High risk operation: require explicit user confirmation before execution.`,
  scopes: ["write", "dangerous"],
  annotations: {
    "title": "Debug Api",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apIsDebugApi(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
