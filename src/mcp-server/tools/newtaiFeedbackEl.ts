/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { newtaiFeedbackEl } from "../../funcs/newtaiFeedbackEl.js";
import { FeedbackElRequest$zodSchema } from "../../models/feedbackelop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FeedbackElRequest$zodSchema,
};

export const tool$newtaiFeedbackEl: ToolDefinition<typeof args> = {
  name: "feedback_el",
  description: `Submit Newt AI generated Expression Language feedback

Submit feedback for the Newt AI generated Expression Language.`,
  scopes: ["write"],
  annotations: {
    "title": "Feedback El",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await newtaiFeedbackEl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
