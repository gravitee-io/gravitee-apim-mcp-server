/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiAnalyticsPerformAPIAnalytics } from "../../funcs/apiAnalyticsPerformAPIAnalytics.js";
import { PerformApiAnalyticsRequest$zodSchema } from "../../models/performapianalyticsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PerformApiAnalyticsRequest$zodSchema,
};

export const tool$apiAnalyticsPerformAPIAnalytics: ToolDefinition<typeof args> =
  {
    name: "perform_api_analytics",
    description: `Perform analytics on API data

Perform analytics on API data based on the provided query parameters.`,
    scopes: ["read"],
    annotations: {
      "title": "Perform Api Analytics",
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await apiAnalyticsPerformAPIAnalytics(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
