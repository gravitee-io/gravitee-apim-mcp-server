/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiMembersGetAPIPrimaryOwner } from "../../funcs/apiMembersGetAPIPrimaryOwner.js";
import { GetApiPrimaryOwnerRequest$zodSchema } from "../../models/getapiprimaryownerop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetApiPrimaryOwnerRequest$zodSchema,
};

export const tool$apiMembersGetAPIPrimaryOwner: ToolDefinition<typeof args> = {
  name: "get_api_primary_owner",
  description: `Get the API's primary owner userId

Get the API Primary Owner userId. If the primary owner is a group, then the userId of the primary owner user within that group will be returned, not the groupId. The userId is wrapper into a Member object, but no other properties are returned.`,
  scopes: ["read"],
  annotations: {
    "title": "Get Api Primary Owner",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiMembersGetAPIPrimaryOwner(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
