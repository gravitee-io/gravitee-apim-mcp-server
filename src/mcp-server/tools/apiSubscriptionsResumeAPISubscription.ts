/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiSubscriptionsResumeAPISubscription } from "../../funcs/apiSubscriptionsResumeAPISubscription.js";
import { ResumeApiSubscriptionRequest$zodSchema } from "../../models/resumeapisubscriptionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ResumeApiSubscriptionRequest$zodSchema,
};

export const tool$apiSubscriptionsResumeAPISubscription: ToolDefinition<
  typeof args
> = {
  name: "resume_api_subscription",
  description: `Resume a paused API's subscription

Resume a previously paused subscription.`,
  scopes: ["write"],
  annotations: {
    "title": "Resume Api Subscription",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiSubscriptionsResumeAPISubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
