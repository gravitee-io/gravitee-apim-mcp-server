/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiDocumentationCreateDocumentationPage } from "../../funcs/apiDocumentationCreateDocumentationPage.js";
import { CreateDocumentationPageRequest$zodSchema } from "../../models/createdocumentationpageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateDocumentationPageRequest$zodSchema,
};

export const tool$apiDocumentationCreateDocumentationPage: ToolDefinition<
  typeof args
> = {
  name: "API-documentation-create-documentation-page",
  description: `Create API documentation page

Create API documentation. The result will be the created page or folder.
Only MARKDOWN and FOLDER types are supported.

User must have the API_DOCUMENTATION[CREATE] permission.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiDocumentationCreateDocumentationPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
