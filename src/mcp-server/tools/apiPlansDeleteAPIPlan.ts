/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiPlansDeleteAPIPlan } from "../../funcs/apiPlansDeleteAPIPlan.js";
import { DeleteApiPlanRequest$zodSchema } from "../../models/deleteapiplanop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteApiPlanRequest$zodSchema,
};

export const tool$apiPlansDeleteAPIPlan: ToolDefinition<typeof args> = {
  name: "delete_api_plan",
  description: `Delete one API's plan

Delete the API's plan.

High risk operation: require explicit user confirmation before execution.`,
  scopes: ["write", "dangerous"],
  annotations: {
    "title": "Delete Api Plan",
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiPlansDeleteAPIPlan(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
