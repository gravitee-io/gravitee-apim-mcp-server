/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiMembersListAPIMembers } from "../../funcs/apiMembersListAPIMembers.js";
import { ListApiMembersRequest$zodSchema } from "../../models/listapimembersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListApiMembersRequest$zodSchema,
};

export const tool$apiMembersListAPIMembers: ToolDefinition<typeof args> = {
  name: "list_api_members",
  description: `Get the API's members

List the members for a given API.`,
  scopes: ["read"],
  annotations: {
    "title": "List Api Members",
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apiMembersListAPIMembers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
