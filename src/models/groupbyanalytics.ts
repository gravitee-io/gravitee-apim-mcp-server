/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { AnalyticsType, AnalyticsType$zodSchema } from "./analyticstype.js";

/**
 * Generic Analytics response
 */
export type GroupByAnalytics = {
  analyticsType: AnalyticsType;
  values?: { [k: string]: number } | undefined;
  metadata?: { [k: string]: { [k: string]: string } } | undefined;
};

export const GroupByAnalytics$zodSchema: z.ZodType<GroupByAnalytics> = z.object(
  {
    analyticsType: AnalyticsType$zodSchema.describe(
      "The type of the analytics",
    ),
    metadata: z.record(z.string(), z.record(z.string(), z.string())).optional(),
    values: z.record(z.string(), z.int()).optional(),
  },
).describe("Generic Analytics response");
