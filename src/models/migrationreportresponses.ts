/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  MigrationStateType,
  MigrationStateType$zodSchema,
} from "./migrationstatetype.js";

export type Issue = {
  message?: string | undefined;
  state?: MigrationStateType | undefined;
};

export const Issue$zodSchema: z.ZodType<Issue> = z.object({
  message: z.string().optional(),
  state: MigrationStateType$zodSchema.optional().describe(
    "* MIGRATED: API is migrated\n* MIGRATABLE: DRY_RUN and can works\n* CAN_BE_FORCED: if you force, we upgrade\n* IMPOSSIBLE: we don’t know howto upgrade\n",
  ),
});

/**
 * Migration report
 */
export type MigrationReportResponses = {
  state?: MigrationStateType | undefined;
  issues?: Array<Issue> | undefined;
};

export const MigrationReportResponses$zodSchema: z.ZodType<
  MigrationReportResponses
> = z.object({
  issues: z.array(z.lazy(() => Issue$zodSchema)).optional().describe(
    "List of issues.",
  ),
  state: MigrationStateType$zodSchema.optional().describe(
    "* MIGRATED: API is migrated\n* MIGRATABLE: DRY_RUN and can works\n* CAN_BE_FORCED: if you force, we upgrade\n* IMPOSSIBLE: we don’t know howto upgrade\n",
  ),
}).describe("Migration report");
