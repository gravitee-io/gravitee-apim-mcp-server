/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ErrorT, ErrorT$zodSchema } from "./error.js";
import { Plan, Plan$zodSchema } from "./plan.js";

export type RequestBody = {};

export const RequestBody$zodSchema: z.ZodType<RequestBody> = z.object({});

export type PatchApiPlanRequest = {
  envId?: string | undefined;
  apiId: string;
  planId: string;
  dryRun?: boolean | undefined;
  ifMatch?: string | undefined;
  RequestBody: Array<RequestBody>;
};

export const PatchApiPlanRequest$zodSchema: z.ZodType<PatchApiPlanRequest> = z
  .object({
    RequestBody: z.array(z.lazy(() => RequestBody$zodSchema)),
    apiId: z.string().describe("Id of an API."),
    dryRun: z.boolean().default(false),
    envId: z.string().default("DEFAULT").describe(
      "Id or Hrid (Human readable Id) of an environment.",
    ),
    ifMatch: z.string().describe(
      "Optional optimistic-concurrency precondition. Supply the plan's current ETag (as returned by a prior read or patch).\nIf the plan was modified since that ETag was read, the request is rejected with 412.",
    ).optional(),
    planId: z.string().describe("Id of a plan."),
  });

export type PatchApiPlanResponseResult = Plan | ErrorT;

export const PatchApiPlanResponseResult$zodSchema: z.ZodType<
  PatchApiPlanResponseResult
> = z.union([
  Plan$zodSchema,
  ErrorT$zodSchema,
]);

export type PatchApiPlanResponse = {
  Headers: { [k: string]: Array<string> };
  Result: Plan | ErrorT;
};

export const PatchApiPlanResponse$zodSchema: z.ZodType<PatchApiPlanResponse> = z
  .object({
    Headers: z.record(z.string(), z.array(z.string())).default({}),
    Result: z.union([
      Plan$zodSchema,
      ErrorT$zodSchema,
    ]),
  });
