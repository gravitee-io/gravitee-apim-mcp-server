/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * Summary of native API connection logs aggregated by connection status.
 */
export type NativeApiLogsSummary = {
  countByConnectionStatus?: { [k: string]: number } | undefined;
};

export const NativeApiLogsSummary$zodSchema: z.ZodType<NativeApiLogsSummary> = z
  .object({
    countByConnectionStatus: z.record(z.string(), z.int()).optional().describe(
      "Number of native connection lifecycle events keyed by connection status (CONNECTED, CONNECTION_ERROR, SESSION_ERROR, INTERNAL_ERROR).",
    ),
  }).describe(
    "Summary of native API connection logs aggregated by connection status.",
  );
