/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { CountAnalytics, CountAnalytics$zodSchema } from "./countanalytics.js";
import {
  GroupByAnalytics,
  GroupByAnalytics$zodSchema,
} from "./groupbyanalytics.js";
import {
  HistogramAnalytics,
  HistogramAnalytics$zodSchema,
} from "./histogramanalytics.js";
import { StatsAnalytics, StatsAnalytics$zodSchema } from "./statsanalytics.js";

/**
 * Successful analytics response
 */
export type ApiAnalyticsResponse =
  | (StatsAnalytics & { analyticsType: "STATS" })
  | (CountAnalytics & { analyticsType: "COUNT" })
  | (HistogramAnalytics & { analyticsType: "HISTOGRAM" })
  | (GroupByAnalytics & { analyticsType: "GROUP_BY" });

export const ApiAnalyticsResponse$zodSchema: z.ZodType<ApiAnalyticsResponse> = z
  .union([
    StatsAnalytics$zodSchema.and(
      z.object({
        analyticsType: z.literal("STATS"),
      }).transform((v) => ({ analyticsType: v.analyticsType })),
    ),
    CountAnalytics$zodSchema.and(
      z.object({
        analyticsType: z.literal("COUNT"),
      }).transform((v) => ({ analyticsType: v.analyticsType })),
    ),
    HistogramAnalytics$zodSchema.and(
      z.object({
        analyticsType: z.literal("HISTOGRAM"),
      }).transform((v) => ({ analyticsType: v.analyticsType })),
    ),
    GroupByAnalytics$zodSchema.and(
      z.object({
        analyticsType: z.literal("GROUP_BY"),
      }).transform((v) => ({ analyticsType: v.analyticsType })),
    ),
  ]).describe("Successful analytics response");
