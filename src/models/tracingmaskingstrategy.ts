/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

/**
 * FULL replaces the entire value; PARTIAL keeps a visible prefix and/or suffix.
 *
 * @remarks
 */
export const TracingMaskingStrategyType = {
  Full: "FULL",
  Partial: "PARTIAL",
} as const;
/**
 * FULL replaces the entire value; PARTIAL keeps a visible prefix and/or suffix.
 *
 * @remarks
 */
export type TracingMaskingStrategyType = ClosedEnum<
  typeof TracingMaskingStrategyType
>;

export const TracingMaskingStrategyType$zodSchema = z.enum([
  "FULL",
  "PARTIAL",
]).describe(
  "FULL replaces the entire value; PARTIAL keeps a visible prefix and/or suffix.\n",
);

export type TracingMaskingStrategy = {
  type: TracingMaskingStrategyType;
  replacement?: string | undefined;
  prefixLength?: number | undefined;
  suffixLength?: number | undefined;
};

export const TracingMaskingStrategy$zodSchema: z.ZodType<
  TracingMaskingStrategy
> = z.object({
  prefixLength: z.int().default(0).describe(
    "PARTIAL only — number of leading characters to keep visible.",
  ),
  replacement: z.string().optional().describe(
    "For FULL: replacement text (default [REDACTED]). For PARTIAL: single mask character (default *).\n",
  ),
  suffixLength: z.int().default(0).describe(
    "PARTIAL only — number of trailing characters to keep visible.",
  ),
  type: TracingMaskingStrategyType$zodSchema.describe(
    "FULL replaces the entire value; PARTIAL keeps a visible prefix and/or suffix.\n",
  ),
});
